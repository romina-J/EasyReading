let ComponentVersionContainerBase = require("./../../base/component-version-container-base");

class PresentationVersionContainer extends ComponentVersionContainerBase{
    constructor(id){
        super(id);
    }
}

module.exports = PresentationVersionContainer;