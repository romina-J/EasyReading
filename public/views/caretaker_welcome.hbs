<!DOCTYPE html>

<html lang="en">

<head>
    <title>{{menu_home_text}}</title>
    {{> head }}
</head>

<body>
    <div class="row wrapper">
        <div class="medium-3 columns">
            <nav>
            {{> nav_backend }}
            {{> nav_user_info }}
            </nav>
        </div>

        <div class="medium-9 columns">
            <main>
                <div class="row column">
                    <h1 id="message">{{e}}</h1>
                </div>

                <div class="row">
                    <div class="column medium-12">
                        <h1 class="page-client-welcome-intro-title">{{er_backend_title}}</h1>

                        <h2>What can you do in the backend?</h2>
                        <ul>
                            <li>
                                Manage your clients(adjust user tools and user interfaces...)
                            </li>
                            <li>
                                Add static translations for webpages for your clients...
                            </li>
                        </ul>

                    </div>
                </div>

                {{#hasRole user "caretaker"}}

                {{else}}
                    {{#hasRole user "client"}}
                    <form id="submit" method="post">

                        <h2>Info</h2>
                        <fieldset  class="fieldset">
                            <legend>You are currently not a caretaker - do you want to upgrade your account?</legend>
                            <input id="upgrade_yes" type="radio" name="upgrade" value="yes" checked> <label for="upgrade_yes"> Yes</label><br>
                            <input id="upgrade_no" type="radio" name="upgrade" value="no"><label for="upgrade_no"> No</label><br>

                            <input type="submit" name="upgrade" value="Submit">
                        </fieldset>


                    </form>
                    {{/hasRole}}
                {{/hasRole}}

            </main>

        </div>

    </div>

</body>
</html>